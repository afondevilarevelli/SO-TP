#include <stdio.h>
#include <stdlib.h>
#include <commons/collections/queue.h>

#define alpha 40;

t_queue algoritmoSJF(t_queue colaEsi);

t_queue algoritmoSRT(t_queue colaESI);

t_queue algoritmoHRRN(t_queue colaESI);

// devuelven la cola ordenada mediante su algoritmo